---
title: A good way to parse documents
date: 2026-04-01
tags: 'hello'
---

Selenium is a regex tool used for webscraping in python. it is pretty good.
anyways.

im pretty sure this is not going to parse but...

for `var` in `x=2`:

```python
for x in len(data):
    count = x+1
```

